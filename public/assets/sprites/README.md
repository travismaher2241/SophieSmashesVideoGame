# Player sprites

The swing frames the game loads, one directory per swing style: `driver/`,
`iron/` (irons and wedges) and `putter/`.

These are generated — do not edit them by hand. The source sheets and the
command that cuts them are in `assets/sprites/`.
